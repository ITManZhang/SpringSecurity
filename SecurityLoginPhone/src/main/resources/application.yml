spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/db_security?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

logging:
  level:
    com.com.paidaxing.security: debug
mybatis-plus:
  global-config:
    db-config:
      id-type: assign_id